<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Database Schema - SQL Injection Lab</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 1000px;
            margin: 0 auto;
            padding: 20px;
            background-color: #F2EFEB;
        }
        .container {
            background: #FFFFFF;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 2px 10px rgba(48, 44, 42, 0.1);
        }
        .schema-info {
            background-color: #F2EFEB;
            border: 1px solid #918B83;
            padding: 20px;
            border-radius: 4px;
            margin-bottom: 20px;
        }
        .schema-info pre {
            background-color: #FFFFFF;
            padding: 15px;
            border-radius: 4px;
            overflow-x: auto;
            font-size: 14px;
            line-height: 1.4;
            border: 1px solid #918B83;
        }
        .nav-links {
            margin-bottom: 20px;
        }
        .nav-links a {
            color: #3C1053;
            text-decoration: none;
            margin-right: 15px;
        }
        .nav-links a:hover {
            color: #B51825;
            text-decoration: underline;
        }
        .info-box {
            background-color: #F2EFEB;
            border: 1px solid #918B83;
            padding: 15px;
            border-radius: 4px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🗄️ Database Schema - SQL Injection Lab</h1>
        
        <div class="nav-links">
            <a href="/">Login</a>
            <a href="/search">Search Users (Vulnerable)</a>
            <a href="/safe-search">Search Users (Safe)</a>
            <a href="/schema">Database Schema</a>
        </div>

        <div class="schema-info">
            <h3>Database Tables:</h3>
            <% if (tables && tables.length > 0) { %>
                <% tables.forEach(table => { %>
                    <pre><%= table.sql %></pre>
                <% }); %>
            <% } else { %>
                <p>No tables found.</p>
            <% } %>
        </div>

        <div class="info-box">
            <h3>📊 Database Information:</h3>
            <p><strong>Database Type:</strong> SQLite (In-Memory)</p>
            <p><strong>Main Table:</strong> users</p>
            <p><strong>Columns:</strong></p>
            <ul>
                <li><code>id</code> - INTEGER PRIMARY KEY AUTOINCREMENT</li>
                <li><code>username</code> - TEXT NOT NULL</li>
                <li><code>email</code> - TEXT NOT NULL</li>
                <li><code>password</code> - TEXT NOT NULL</li>
                <li><code>role</code> - TEXT DEFAULT 'user'</li>
            </ul>
            <p><strong>Sample Data:</strong> 4 users (1 admin, 3 regular users)</p>
        </div>

        <div class="info-box">
            <h3>💡 SQL Injection Learning Tips:</h3>
            <ul>
                <li><strong>Understand the schema:</strong> Knowing table and column names helps craft better injection attacks</li>
                <li><strong>Use UNION attacks:</strong> Combine results from different tables</li>
                <li><strong>Extract metadata:</strong> Use <code>sqlite_master</code> to discover database structure</li>
                <li><strong>Test different payloads:</strong> Try various SQL injection techniques</li>
                <li><strong>Compare vulnerable vs safe:</strong> See how parameterized queries prevent injection</li>
            </ul>
        </div>
    </div>
</body>
</html>
