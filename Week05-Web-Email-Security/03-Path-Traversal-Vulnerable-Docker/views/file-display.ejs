<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            background-color: #F2EFEB; /* Soft Ivory */
        }
        .container {
            background: #FFFFFF; /* White */
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 2px 10px rgba(48, 44, 42, 0.1); /* Deep Charcoal shadow */
        }
        h1 {
            color: #3C1053; /* Purple */
            text-align: center;
            margin-bottom: 30px;
        }
        .status {
            padding: 15px;
            border-radius: 8px;
            margin: 20px 0;
            font-weight: bold;
        }
        .vulnerable {
            background-color: #F2EFEB; /* Soft Ivory */
            border: 2px solid #F2120C; /* Red */
            color: #F2120C; /* Red */
        }
        .safe {
            background-color: #F2EFEB; /* Soft Ivory */
            border: 2px solid #27ae60;
            color: #229954;
        }
        .file-info {
            background-color: #F2EFEB; /* Soft Ivory */
            padding: 15px;
            border-radius: 4px;
            margin: 15px 0;
            border: 1px solid #918B83; /* Warm Stone */
            color: #302C2A; /* Deep Charcoal */
        }
        .file-content {
            background-color: #302C2A; /* Deep Charcoal */
            color: #F2EFEB; /* Soft Ivory */
            padding: 20px;
            border-radius: 4px;
            font-family: 'Courier New', monospace;
            white-space: pre-wrap;
            overflow-x: auto;
            margin: 15px 0;
        }
        .back-button {
            background-color: #3C1053; /* Purple */
            color: #FFFFFF; /* White */
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            text-decoration: none;
            display: inline-block;
            margin: 10px 0;
        }
        .back-button:hover {
            background-color: #B51825; /* Purple (Faculty of Law and Business) */
        }
        .warning {
            background-color: #F2EFEB; /* Soft Ivory */
            border: 1px solid #F2120C; /* Red */
            padding: 15px;
            border-radius: 4px;
            margin: 15px 0;
            color: #302C2A; /* Deep Charcoal */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>📄 File Content Display</h1>
        
        <div class="status <%= vulnerable ? 'vulnerable' : 'safe' %>">
            <% if (vulnerable) { %>
                ⚠️ VULNERABLE: This file was accessed using the vulnerable endpoint
            <% } else { %>
                ✅ SAFE: This file was accessed using the secure endpoint
            <% } %>
        </div>

        <div class="file-info">
            <strong>Filename:</strong> <%= filename %><br>
            <strong>Access Method:</strong> <%= vulnerable ? 'Vulnerable (Direct Path)' : 'Safe (Validated Path)' %>
        </div>

        <% if (vulnerable) { %>
            <div class="warning">
                <strong>⚠️ Security Warning:</strong> This file was accessed through a vulnerable implementation that doesn't validate file paths. In a real application, this could allow access to sensitive system files.
            </div>
        <% } %>

        <h3>File Content:</h3>
        <div class="file-content"><%= content %></div>

        <a href="/" class="back-button">← Back to Home</a>
    </div>
</body>
</html>
